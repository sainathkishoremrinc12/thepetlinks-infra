apiVersion: v1
kind: Service
metadata:
  name: {{ include "mail-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4110
      protocol: TCP
      name: http
  selector:
    app: mail-service
