apiVersion: v1
kind: Service
metadata:
  name: {{ include "role-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: role-service
