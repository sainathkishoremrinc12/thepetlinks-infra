apiVersion: v1
kind: Service
metadata:
  name: {{ include "master-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4104
      protocol: TCP
      name: http
  selector:
    app: master-service
