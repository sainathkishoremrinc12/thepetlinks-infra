apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4017
      protocol: TCP
      name: http
  selector:
    app: customer-service
