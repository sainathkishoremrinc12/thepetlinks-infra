apiVersion: v1
kind: Service
metadata:
  name: {{ include "vendor-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4106
      protocol: TCP
      name: http
  selector:
    app: vendor-service
