apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4013
      protocol: TCP
      name: http
  selector:
    app: gateway


