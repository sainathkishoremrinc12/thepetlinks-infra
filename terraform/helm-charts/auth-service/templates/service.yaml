apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-service.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4102
      protocol: TCP
      name: http
  selector:
    app: auth-service
