apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: kafka
spec:
  clusterIP: None   # headless service
  selector:
    app: kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092